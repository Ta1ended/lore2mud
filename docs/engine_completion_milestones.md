# Engine Completion Milestones

_Last updated: 2026-07-28（M1 验收返工）_

本文件定义公共引擎从当前状态到功能完备的里程碑路线。每个里程碑是一个可独立验证的
纵向切片，完成后更新本文件和相关交接记录。

---

## M1: 死亡 / 失败处理

- **状态**: 已由 Hermes 实现并完成执行者自测，等待 GPT-5.6-sol 独立验收。
- **日期**: 2026-07-28
- **内容**: `World.recover()` 将死亡玩家传送回起始房间并恢复满血；`_require_alive()`
  统一门禁覆盖 10 个修改型方法；命令层死亡门禁（`_DEAD_ALLOWED`）位于裸对话选项
  和 `bye` 路由之前；`RecoverOutcome` typed result；save/load 往返验证；55 项测试。
- **执行者自测**: 470 项测试通过、编译、内容校验、安全扫描、真实 CLI 冒烟。
- **待验收**: GPT-5.6-sol 独立复核。

## M2: 全场 typed stacks 物品数量系统

- **状态**: 待 M1 验收通过后开始
- **内容**: 为物品引入 typed stacks 数量系统，背包内物品可携带数量；升级存档格式至
  v6，严格拒绝 v5 存档；内容包版本同步升级。
- **验收**: v5 存档被明确拒绝、新格式往返验证、全量回归。

## M3: 三类任务系统

- **状态**: 规划中
- **内容**: 实现 `monster_defeated`、`reach_room`、`collect_item` 三种任务触发条件，
  扩展 `QuestDefinition` 和 `World` 任务评估逻辑。
- **验收**: 每种任务类型有独立测试覆盖、save/load 往返、内容校验。

## M4: 强类型对话效果

- **状态**: 规划中
- **内容**: 为对话选项实现强类型效果：`grant_experience`、`accept_quest`、`set_flag`，
  扩展 `DialogueOption` 和 `World.select_option()` 的效果处理。
- **验收**: 每种效果有独立测试、状态不变性、save/load 往返。

## M5: 固定金币商店

- **状态**: 规划中
- **内容**: 实现金币系统和固定商店 NPC，玩家可买卖物品。
- **验收**: 金币增减正确、买卖交易测试、save/load 往返。

## M6: Examine、帮助和错误反馈

- **状态**: 规划中
- **内容**: 完善 `examine` 命令（查看怪物/NPC 详情）、优化 `help` 上下文帮助、
  改进错误反馈的用户友好性。
- **验收**: 帮助文本覆盖所有命令、错误消息一致性。

## M7: 原创内容规模验收

- **状态**: 规划中
- **内容**: 扩展原创演示至至少 8 个房间、4 只怪物、4 条任务，覆盖全部核心系统
  （物品、装备、对话、任务、商店）。内容扩容队列第一步：新增第二原创遭遇（房间 +
  怪物 + 任务）。
- **验收**: 内容包规模达标、全量回归通过、真实 CLI 长流程试玩。

## M8: 公共引擎完成独立审计

- **状态**: 未开始
- **内容**: 所有 M1–M7 里程碑完成后，进行独立 GPT-5.6-sol 公共引擎 readiness
  audit。
- **验收**: 全量测试通过、独立审计 GO、无已知引擎缺口。

---

## 安全边界：私有小说事实层

私有小说事实层**不属于** M1–M8 公共引擎路线。它只能在 M8 独立审计通过后，由项目
负责人提供新的明确范围授权后开始。公共引擎完成状态不能自动推导为私有事实层授权。
