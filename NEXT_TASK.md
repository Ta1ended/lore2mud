# Next Task

_Last updated: 2026-07-28_

## Status

本地已完成并验证确定性单物品怪物战利品：原创演示怪物首次被击败时，将其唯一、初始未摆放的
`loot_item_id` 放入当前房间；玩家仍通过既有 `look` / `take` 使用它。内容包为 v0.2.7，save
JSON 格式仍为 v5；存活怪物战利品若已出现在保存的房间或背包中，读档会拒绝该不可能状态。
15 项 `tests/test_loot.py` 测试、完整 415 项测试、编译、原创内容校验和真实
`击败 → look → take` CLI 试玩均于 2026-07-28 通过。

远端 `main` 在本切片开始前经本地跟踪和直接查询均为
`1936e913348d3d46278ffaae2cfabf6502020835`；此前“尚未 push / 下一步发布”的交接描述已过期。
本地提交绝不自动推送，恢复时必须以 `git status --short --branch`、
`git rev-list --left-right --count HEAD...origin/main` 和直接远端查询重新确认实时状态。

本切片依项目负责人的临时流程例外由 Codex / GPT-5.6-terra 自审并执行；这不是独立
GPT-5.6-sol 验收。全程仅处理公共引擎和原创演示，未读取、复制、扫描或接入私有小说资料。
415 项公共回归测试是稳定性证据，不是私有小说事实层的准确性认证或访问许可。

## Single next action

执行一次**只读的公共核心稳定性 readiness audit**：针对当前公共引擎和原创演示，以明确的
稳定性门槛审查命令错误路径与状态不变性、save v5 契约、内容加载/跨引用、安全历史门、完整测试和
真实 CLI 游戏循环，并记录 `GO`、`CONDITIONAL GO` 或具体 blocker。不得修改代码、不得扩大内容、
不得读取、扫描或接入 `D:\MUD game kaifa\小说` 的任何内容。

## Acceptance criteria

- 所有结论必须由本次实时 Git、代码、测试和公开原创演示证据支撑，并明确区分历史记录与实时状态。
- 定义“可扩充公共可玩内容”的最小门槛，说明哪些已满足、哪些仅有回归证据、哪些仍是风险。
- 核对 save v5 的恢复边界、失败原子性和内容包版本绑定；不得借审计升级存档格式或重构。
- 核对公开仓库安全门和可达历史扫描，不访问私有路径，也不把私有事实、canon、摘要或派生内容写入仓库。
- 给出唯一的后续建议：若为 GO 或可控条件 GO，下一代码切片只能扩展公共、完全原创的可玩内容；
  私有事实层无论审计结果如何，仍须项目负责人另行明确、范围受限地授权后才能开始。
- 同步本次只读审计的 handoff 记录；不自动 `git push`。
